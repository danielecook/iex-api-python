language: python
cache: pip
python:
  - "3.6"

before_install:
  - pip install pytest
  - pip install pytest-cov
  - pip install coveralls
script:
  - py.test
after_success:
  - COVERALLS_REPO_TOKEN=${COVERALLS_REPO_TOKEN} coveralls